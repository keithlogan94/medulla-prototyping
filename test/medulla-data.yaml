apiVersion: medulla.recro.com/v1alpha1
kind: Data
metadata:
  name: test-1
databases:
  - name: test
    host: my-host.app
    dialect: mysql
    usernameSecretName: test-secret
    passwordSecretName: test-secret
    models:
      - name: User
        columns:
          - columnName: firstName
            type: string
            allowNull: false
            defaultValue: ""
            autoIncrement: true
            primaryKey: true
            field: "test_field"
            unique: "uniqueField"
            comment: "This is a Test"
      - name: Account
        columns:
          - columnName: firstName
            type: string
            allowNull: false
            defaultValue: ""
            autoIncrement: true
            primaryKey: true
            field: "test_field"
            unique: "uniqueField"
            comment: "This is a Test"
          - columnName: lastName
            type: string
            allowNull: false
            defaultValue: ""
            autoIncrement: true
            primaryKey: true
            field: "test_field"
            unique: "uniqueField"
            comment: "This is a Test"
